name: DUC_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # <-- Gói này vẫn cần thiết
    sdk: flutter
  intl: ^0.20.2 # <-- Gói này vẫn cần thiết
  provider: ^6.1.2
  cupertino_icons: ^1.0.2
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true