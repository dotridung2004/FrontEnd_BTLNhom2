name: btl_nhom2
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2 # (Hãy đảm bảo phiên bản SDK phù hợp với Flutter của bạn)

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  image_picker: ^1.0.7 # ✅ ĐÃ THÊM DÒNG NÀY

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # (Hãy đảm bảo phiên bản phù hợp)

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf