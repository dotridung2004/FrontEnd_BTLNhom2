name: btl_nhom2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Dải phiên bản SDK linh hoạt hơn

dependencies:
  flutter:
    sdk: flutter

  # Gói cho bản địa hóa và định dạng ngày tháng
  flutter_localizations: # ✅ ĐÃ THÊM
    sdk: flutter         # ✅ ĐÃ THÊM
  intl: ^0.20.2        # ✅ ĐÃ THÊM

  # Các gói bạn đã có
  fl_chart: ^0.68.0
  cupertino_icons: ^1.0.8
  http: ^1.2.1

  # Gói cho việc chọn ảnh và lưu trữ
  image_picker: ^1.0.7
  shared_preferences: ^2.2.3         # ✅ ĐÃ THÊM
  flutter_secure_storage: ^9.0.0     # ✅ ĐÃ THÊM

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true