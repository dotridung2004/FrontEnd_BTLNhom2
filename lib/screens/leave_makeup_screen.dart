import 'package:flutter/material.dart';

class LeaveMakeupScreen extends StatelessWidget {
  const LeaveMakeupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Chức năng Nghỉ/Bù',
        style: TextStyle(fontSize: 20, color: Colors.grey),
      ),
    );
  }
}